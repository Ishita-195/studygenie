# Keep Apache's memory footprint small on the free tier.
<IfModule mpm_prefork_module>
    StartServers            2
    MinSpareServers         1
    MaxSpareServers         3
    MaxRequestWorkers       8
    MaxConnectionsPerChild  500
</IfModule>
